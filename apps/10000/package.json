{
  "name": "1000",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:vite": "vite build",
    "build:farm": "farm build",
    "build:rsbuild": "rsbuild build",
    "build:rolldown": "rolldown build --config rolldown.config.mjs",
    "build:rspack": "rspack build --config rspack.config.mjs",
    "build:rollup": "rollup --config rollup.config.mjs",
    "build:esbuild": "esbuild --bundle --outdir=dist-esbuild --platform=node --target=es2015 ./src/index.jsx --minify",
    "build:bun": "bun build --outdir=dist-bun ./src/index.jsx --production --sourcemap"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {}
}
