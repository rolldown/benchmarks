{
  "name": "rome-ts",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build:rolldown": "rolldown build --config rolldown.config.mjs",
    "build:rspack": "rspack build --config rspack.config.mjs",
    "build:rollup": "rollup --config rollup.config.mjs",
    "build:esbuild": "esbuild ./src/entry.ts --bundle --outdir=dist-esbuild --platform=node --minify --sourcemap",
    "build:bun": "bun build ./src/entry.ts --outdir=dist-bun --production --sourcemap"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {}
}
